# AI 搭配实施与验证记录

## 隔离与范围

- 工作区：`/Users/neihe/.codex/worktrees/b02e/NEIHE-AI`。
- 分支：`codex/ai-styling-template`，基线 `14be0b4e7b28087cc9031ddb3e7c96de51f67060`。
- `ui-design` 仍在原工作区和原分支；未对该工作区执行修改、切换、构建或部署。
- 保留本工作区原有色库、品牌色、部署和依赖改动；AI 搭配迁移 21 可独立从当前 `main` 的迁移 18 应用，本次提交不包含迁移 19、20 或颜色管理代码。

## 已实现

- 内置模板 `builtin-tool-ai-styling`：上传参考图 → AI 搭配 → 搭配结果。
- 多角度上传、主图选择、显式识别、保留上装/下装/连体/整套、五个独立单品开关、可选补充要求，以及 1/2/4 套生成。
- 使用本地 shadcn 控件和已有节点主题；保持 280/320px 节点宽度与 80px 间隔。
- 服务端识别归属/图片指纹校验、请求去重、跨项目成功缓存、GET 状态恢复；禁止绕过识别从直接生成接口提交。
- 多套串行生成，以首套维持人物与背景一致性；逐套 PostgreSQL 检查点、取消与租约恢复、未知结果停止重发。
- 即时成功结果、部分失败/未知、跨项目 Results 和固定结果节点；重复生成不自动新增节点。
- 文档快照白名单、输入变更失效、请求修订号和文档目标校验，防止迟到响应覆盖新输入。

## 验证证据

- 新增 `tests/ai-styling.test.ts`、`tests/styling-backend.test.ts`、`tests/styling-queue.test.ts`，已在隔离 PostgreSQL runner 内通过。
- 覆盖识别 POST/GET、同图去重/跨项目缓存、非法图片及过期识别、串行生成、部分失败/未知、取消、Worker 租约过期与首套检查点恢复。
- 新增桌面 E2E 覆盖真实素材上传与项目保存、模拟识别和 SSE 生成、五按钮键盘切换、主图变更失效、重复生成及固定三节点、部分未知保留。
- 三宽度（1024/1280/1440）的搭配流程及已有节点几何、结果操作回归共 22 项通过。
- 最后一次专项浏览器复验 4 项通过（含登录准备及三宽度）；补充按钮对比度 ≥ 4.5 断言，并人工检查了 1024px 成功截图。截图使用白色模拟图片，不代表真实 AI 效果。
- `npm run build` 通过（包含 TypeScript、前端 CSS/包体预算及服务端构建）；初始 JS gzip 为 165,811 / 210,000 bytes。
- 独立只读审查发现的接口绕过、历史状态丢失、外套规则不一致、并发记录覆盖、实时成功卡、迟到识别及用户模板敏感引用残留问题均已修复，并补充回归断言。
- 已执行 CodeGraph sync/affected，包含新增源文件；图影响涉及 180 个依赖节点，结合现有文档、运行队列、历史、权限和桌面测试补充验证。

## 尚未通过或未验证

- `gate:codex` 仍调用 GitNexus，依 AGENTS.md 未运行；不能宣称完整交付门禁通过。
- 未调用真实付费 AI。服饰细节保真、人物和背景一致性仅实现约束与引用策略，真实视觉效果仍需获得授权后单独验收。
