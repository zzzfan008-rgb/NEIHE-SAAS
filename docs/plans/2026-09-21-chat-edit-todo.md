# 侧栏对话修改：开发 TODO 与验收追踪

日期：2026-09-21；状态：**P0–P7 已完成；P8 本地技术门禁已完成，A01–A65 逐条证据和真实 Provider 验收仍未完成**。
依据：[PRD](/Users/neihe/NEIHE-AI/docs/侧栏对话修改PRD.md)、[开发计划](/Users/neihe/NEIHE-AI/docs/plans/2026-09-21-chat-edit-development-plan.md)、[开发周期](/Users/neihe/NEIHE-AI/docs/plans/2026-09-21-chat-edit-schedule.md)。

## 使用规则

- 主负责人按工作包认领；只有代码、测试与证据齐备才能勾选。部分完成用备注说明，不勾选。
- 测试项主归属只是实现责任，最终由 P8 统一复验；PRD 变更须同步本清单和估算。
- 证据模板：负责人 / 基线 SHA / 完成 SHA或未提交差异范围 / 命令 / 结果 / 截图或日志路径 / 未验证事项。
- mock、真实付费效果、正式部署三种状态分开记录，不将文档创建标记为功能完成。
- 每包改现有符号前 CodeGraph impact；新文件也进入交付 affected 清单；空图结果补源码审查。

## 当前批次证据

- 基线 SHA：`be82cb912bfb86c5ef3dddb05830635db51b9a60`；保留既有 dirty 变更，本批新增 P0 设计稿、共享类型、纯规则、PostgreSQL 存储/API 及测试。
- P0/P1 聚焦验证：`pnpm exec tsx tests/image-conversation-rules.test.ts`（先失败于缺失模块，补实现后通过）；`pnpm run lint`（通过）；`git diff --check`（通过）。
- CodeGraph：`codegraph sync .` 已同步 3 个新增文件；`codegraph affected src/types/imageConversation.ts src/lib/imageConversationRules.ts tests/image-conversation-rules.test.ts -p . -j` 仅识别本批纯规则测试为受影响测试。
- P2 聚焦验证：`pnpm run test` / `pnpm run check`（隔离 PostgreSQL；既有套件、`image-conversation-storage` 6 项、`image-conversation-api` 6 项全部通过）；资产来源引用保护、资产预览重读、`/resolve` 完整历史、跨账号 404、历史删除接口缺失均有测试覆盖。
- P3 聚焦验证：同一 `pnpm run test` 中 `image-conversation-planner` 通过，`image-conversation-api` 5 项通过；覆盖默认/明确 5 张/三效果/授权补足、数量澄清、9 张拒绝、单图多项修改、结构化响应错误、超时、重复澄清回答和输入控制字段拒绝。
- P4 聚焦验证：同一 `pnpm run test/check` 中 `image-conversation-queue` 通过，覆盖原子入队回滚、不同对话并发、同一请求幂等、3 成功/2 失败、两项独立手动重试、重复点击、输出回写重放和未知结果阻断；`image-conversation-storage` 6 项、`image-conversation-api` 6 项通过。
- P5/P6/P7 聚焦验证：图片对话输入/恢复/UI 契约、planner、queue、storage/API 均通过；本轮补齐具体结果来源链要求恢复、历史轮次有序输入/蒙版快照、来源轮次、折叠参数、资产预览重读、无历史图片切换隔离、显式模型路由、未知结果活动锁、服务端共享参数能力校验、按模式模型列表、多选图片显式起点、草稿底图冲突确认、同/跨项目来源隔离、三模式草稿隔离和开始新修改独立会话；`pnpm run build`、`pnpm run test`、`pnpm run check` 通过；对话 Dock、多选起点、持久化历史、结果操作、来源隔离、模式草稿、开始新修改、提交契约和运行态/澄清/失败未知结果 E2E 在 1024/1280/1440 均通过，全部图片对话专项共 34/34；其中提交契约回归验证空白指令、三模式草稿/输入顺序、未保存蒙版、参数请求和单次提交，并修复浏览器 `crypto.randomUUID` 的 `Illegal invocation`，运行态回归验证活动轮次编辑锁和失败/未知结果操作边界；CodeGraph 最终状态会在本轮收尾同步后更新，`git diff --check` 通过。
- 未验证/未完成：真实 UI 几何以外的完整 A01–A65 逐条证据、真实文本模型能力/成本及付费调用；`gate:codex` 仍依赖 GitNexus，本项目规则禁止运行；生产迁移、提交推送、合并、发布和部署未执行。

## 一、执行前关卡

- [ ] 确定批准的开发基线及本批文件清单，保留其他 dirty 变更。
- [ ] 确认 P0 的 D01/D06/D07 技术契约与具体 UI 稿。
- [ ] 确认无画布节点承载的原对话重新打开入口，不擅自新增会话列表。
- [ ] 明确加入画布的节点类型、mask 参考上限和键盘/参数区建议。
- [ ] 明确澄清/结果未知的活动锁、恢复与终态策略。
- [ ] 验证 Node >=22.20.0、依赖与隔离 PostgreSQL/Docker 测试入口可用。
- [ ] 将新增服务端测试注册 test:suite，自动测试使用 mock，不带真实 AI 密钥。

## 二、工作包 TODO

### P0 技术契约与 UI 验收稿

依赖：无；2–3 人日；负责人：Codex；状态：已完成（模型提供方和真实成本证据明确留待 P3，不伪装为已验证）。

- [x] P0.1 绘制对话、轮次、具体结果、执行尝试的关联和状态转换，列出 API 请求/响应及错误码。
- [x] P0.2 确定来源标识、原子提交/并发锁、澄清持久化、未知结果恢复及旧对话重新打开入口。
- [x] P0.3 给出 1024/1280/1440 的布局、宽度、折行、空/多选/失败态和键盘行为稿。
- [x] P0.4 确认技术方案和具体 UI 稿；结构化规划器、严格 JSON 校验、最多一次规划请求、超时/成本配置、mock 边界已记录；具体文本模型/真实成本证据留待 P3，不伪装成已验证。
- [x] P0.V 执行计划中的验证并记录证据，检查完整差异。

### P1 共享契约、校验与上下文纯逻辑

依赖：P0；2–3 人日；负责人：Codex；状态：已完成。

- [x] P1.1 编写单图 1 张、融合 2–8、输出整数 1–8、无数量控件契约的失败测试。
- [x] P1.2 编写来源链继承、明确覆盖、相对动作不重复、模式隔离及模型不兼容的失败测试。
- [x] P1.3 实现最小类型和纯校验，持久保持要求与本轮相对动作分开，按具体结果而非仅轮次选上下文。
- [x] P1.4 运行纯测试，确保 3/5/6/7 张不取整、超限不截断；请求快照采用深拷贝并冻结，提交后不受草稿变更影响。
- [x] P1.V 执行计划中的验证并记录证据，检查完整差异。

### P2 PostgreSQL 对话存储、身份与授权

依赖：P1；3–4 人日；负责人：Codex；状态：已完成。

- [x] P2.1 先写迁移重复执行、历史快照、猜测标识越权、图片引用归属的失败测试。
- [x] P2.2 实现对话/轮次/意图/尝试及任务关联的增量迁移；不建立第二套文件、计费或生成任务存储。
- [x] P2.3 实现同项目复制共享来源、替换新图独立、跨项目不继承及开始新修改保留旧对话。
- [x] P2.4 测试项目删除/回收、资产引用保护、刷新恢复和跨账号 404；不增加删除对话或历史接口。
- [x] P2.V 执行计划中的验证并记录证据，检查完整差异。

### P3 自然语言意图解析、澄清与要求继承

依赖：P1、P2；P0 解析方案确认；4–6 人日；负责人：Codex；状态：已完成。

- [x] P3.1 建立中文 fixture：默认 1、明确 5、三效果 3、数量不符澄清、授权补足、9 张拒绝、一图多项修改。
- [x] P3.2 注入 mock 解析器，测试结构化响应错误、超时、重复澄清回答与输入注入不能绕过数量/权限。
- [x] P3.3 实现结构化解析和服务端校验，保留原文、结果意图标签、来源结果与有效要求快照；不以关键词拆句替代完整需求。
- [x] P3.4 持久化已提交的澄清请求；明确才创建图片任务。选择旧结果只继承其来源链，失败重试不重新解析目标。
- [x] P3.V 执行计划中的验证并记录证据，检查完整差异。

### P4 持久队列、多方案执行及失败项重试

依赖：P2、P3；3–4 人日；负责人：Codex；状态：已完成。

- [x] P4.1 事务回滚不遗留任务、同一请求并发只接收一轮、幂等冲突拒绝；提交响应丢失可通过原请求号恢复。
- [x] P4.2 按独立意图执行而非串联图片；原子建立轮次与既有队列任务，稳定关联具体输出。
- [x] P4.3 保持明确模型选择及质量/分辨率参数贯穿侧栏入队；不改变其他节点默认路由。
- [x] P4.4 覆盖持久队列重启/重放基础能力、3 成功 2 失败只重试两项、未知结果核对、重复点击重试及输出不重复记账。
- [x] P4.V 执行计划中的验证并记录证据，检查完整差异。

### P5 前端状态与右侧对话壳

依赖：P1；P0 UI 稿确认；可与 P2–P4 并行；3–4 人日；负责人：Codex；状态：已完成。

- [x] P5.1 面板互斥、不持久化开关、三模式草稿独立和迟到回调隔离已实现；结果继续修改的换底图确认取消/确认已由三档 E2E 覆盖，取消保留草稿、确认才替换底图并清空当前模式草稿。
- [x] P5.2 本地开关及独立对话状态不进入 DocumentSnapshot；异步回写绑定 tabId/projectId/documentEpoch。
- [x] P5.3 使用本地 shadcn 组装入口、模式、历史和底部输入，覆盖空选、多选指定起点和模型参数校验。
- [x] P5.4 三档桌面 E2E 验证占位宽度、画布变换不重写、输入/焦点/模式草稿；IME/减少动画未逐条实测。
- [x] P5.V 聚焦测试、build 与三档 E2E 已记录在验收文档。

### P6 四类图片输入与局部重绘

依赖：P5；服务端接口 P2；2–3 人日；负责人：Codex；状态：已完成。

- [x] P6.1 有序引用、预览与稳定身份分离、图片上传归一化和换底图蒙版失效已覆盖。
- [x] P6.2 已接通画布、本地、素材库和当前历史，区分底图/参考图并绑定项目资产引用。
- [x] P6.3 复用 MaskEditor、蒙版上传和既有 alpha PNG/尺寸/体积边界。
- [x] P6.4 上传、蒙版保存和素材选择均有 DocumentTarget 迟到回写门禁；局部重绘参考图可选、蒙版必需。
- [x] P6.V 输入/蒙版专项、全量专项和三档 E2E 证据已记录。

### P7 历史恢复、结果操作与端到端联调

依赖：P4、P5、P6；3–4 人日；负责人：Codex；状态：已完成。

- [x] P7.1 已覆盖输出来源恢复、生成输出映射、同项目来源解析、跨项目隔离和历史快照恢复契约。
- [x] P7.2 本地服务与 mock provider 的轮次、attempt、generation output 关联已接通；未调用真实付费 Provider。
- [x] P7.3 已实现查看、对比、下载、继续修改、作为参考、加入画布和显式打开原对话；加入画布保存稳定来源元数据。
- [x] P7.4 已接入失败意图手动重试、未知结果阻断和澄清回答；费用/真实模型效果仅保留服务端既有提示边界，未做付费验收。
- [x] P7.V `image-conversation-recovery`、全量专项中的对话测试、build 和三档 E2E 已记录。

### P8 全量回归、安全审查与交付

依赖：P0–P7 完成；2–3 人日；负责人：Codex；状态：本地技术门禁完成，验收矩阵和真实 Provider 仍留边界。

- [ ] P8.1 A01–A65 尚未逐条完成证据矩阵；已明确未验证项与阻塞项。
- [x] P8.2 `pnpm run lint`、`pnpm run build`、`pnpm run test`、`pnpm run check` 通过；未修改用户既有 dirty 文件。
- [x] P8.3 CodeGraph `status/sync/affected`、完整差异检查已执行；`gate:codex` 按规则未运行并已记录。
- [x] P8.4 验收报告已形成；真实 AI、提交推送、合并和部署均未执行。
- [x] P8.V 全量退出码已确认；A01–A65 逐条截图/trace 矩阵仍未形成。

## 三、确认需求覆盖 C01–C41

以下勾选代表该需求已实现并验证，而不是用户是否确认；用户已确认全部 C 项。

- [ ] C01 在画布右侧现有「属性」按钮下方新增「对话修改」按钮。
- [ ] C02 点击「对话修改」按钮后，从右侧展开侧栏。
- [ ] C03 「属性」与「对话修改」共用右侧区域，切换显示。
- [ ] C04 顶部提供「单图修改」「多图融合」「mask 局部重绘」三种模式，由用户主动选择。
- [ ] C05 侧栏中间为可滚动的历史框，展示当前对话各轮修改指令和生成图片。
- [ ] C06 历史框不是多个历史会话的列表，不提供会话列表切换。
- [ ] C07 底部固定本轮参考图片、修改指令输入框和发送按钮；局部重绘模式提供蒙版入口。
- [ ] C08 用户可选择历史中的某轮结果，继续修改该图片，之前的版本保留。
- [ ] C09 不同图片各自拥有独立对话历史；同一画布内修改图片 A 与图片 B 时，分别展示各自历史，不合并成项目级对话。
- [ ] C10 从图片 A 的对话加入参考图 B 融合得到 C，这轮融合及对 C 的继续修改仍留在 A 的对话；B 的独立对话历史不受影响。
- [ ] C11 普通点击画布图片只改变选择，不切换当前对话；选中其他图片后明确点击「对话修改」才切换，并保留原对话未发送的草稿。
- [ ] C12 每轮成功结果先显示在当前对话历史中；用户点击「加入画布」才将所选结果加入画布，原图保留，不自动为每轮结果新增画布节点。
- [ ] C13 将结果 C 加入画布后，选中 C 并点击「对话修改」，回到生成 C 的原对话，并以 C 作为本轮修改底图，不另建独立对话。
- [ ] C14 刷新页面或关闭项目后重新打开，恢复各图片已提交的对话历史，包括修改指令、输入图片和生成结果。未发送草稿保留在当前页面，第一版不要求刷新后或跨设备恢复。
- [ ] C15 侧栏支持四种图片来源：画布中的具体图片、本地上传、素材库、当前对话的历史结果。
- [ ] C16 多图融合第一版采用 2–8 张输入图片，8 张为本轮全部输入的总上限，不额外增加底图名额。此项按用户对推荐方案回复「允许」记录。
- [ ] C17 mask 局部重绘支持添加选填参考图；不添加参考图时，仅凭有效底图、蒙版和修改指令也可运行。
- [ ] C18 侧栏默认使用 Sunburst，允许用户切换模型；选择列表仅展示项目已接入且支持当前编辑模式的模型，实际调用须遵循用户明确选择。
- [ ] C19 默认画质为中等（medium），用户可手动调整；只显示所选模型支持的画质档位，高档位由用户主动选择。
- [ ] C20 不提供生成数量选项；默认生成 1 张，用户通过自然语言指定多张输出及各自预期效果，支持 1–8 的任意整数，不限定为 1/2/4/8 档位。
- [ ] C21 支持一轮多方案生成，例如一次提出正面、侧面、微侧面等不同视角要求；各张结果分别体现对应意图，供用户同时比较，而非简单重复同一指令或默认合成为一张拼图。
- [ ] C22 张数与方案描述不一致时先简短询问，不擅自补足；用户明确授权「其他由你安排」时，系统可在授权范围内补充方案。要求清楚时直接生成，不强制逐轮确认。
- [ ] C23 用户明确列出多个独立效果但未写张数时，每种效果生成 1 张，总张数等于效果数量，无需再询问数量；未表达多个效果或多张意图时默认 1 张。
- [ ] C24 单轮最多输出 8 张；明确指定或根据效果列表推导的数量超过 8 张时，提示用户缩减或拆分，不自动截断、不自动分批生成。
- [ ] C25 单图修改默认跟随底图比例，多图融合默认跟随图 1 比例，两者允许手动选择其他比例；mask 局部重绘保持底图比例，不提供改变比例的操作。
- [ ] C26 默认输出分辨率为 2K，允许切换至 4K，仅显示当前模型支持的档位；分辨率与画质分别设置。
- [ ] C27 同一图片对话生成期间允许编辑下一轮草稿，但当前轮结束前不能再次发送；其他图片对话可按项目既有并发限制生成。
- [ ] C28 继续修改时结合选定的上轮结果图、此前仍然有效的要求与本轮增量指令；例如保持背景不变的要求继续生效，在该结果上将袖子再缩短。
- [ ] C29 最新明确指令覆盖对应的冲突旧要求，其他有效要求继续保留；只有含糊、无法确定用户意图时才追问，不要求用户先解除旧要求。
- [ ] C30 选择历史结果继续修改时，继承该结果及其来源链上仍有效的要求，不带入之后轮次或其他分支的改动；已有结果全部保留。
- [ ] C31 同一图片对话的单图修改、多图融合、局部重绘各自保留未发送草稿，包括文字、选图与适用的蒙版；切回恢复，仅限当前页面，不自动发送。
- [ ] C32 当前模式已有未发送草稿时，选择另一张历史结果作为底图，提示「取消切换」或「更换底图并清空当前模式草稿」；仅确认后更换并清空当前模式的文字、旧选图和蒙版，其他模式草稿不受影响。
- [ ] C33 同一项目内复制图片节点，副本与原图共享同一图片来源的对话；副本重新上传替换为另一张图片后建立独立对话，原对话及历史保留。
- [ ] C34 将图片复制到另一个项目时，只带图片，在目标项目建立独立对话，不继承原对话历史和有效要求；原项目及其对话不受影响。
- [ ] C35 从本地上传或素材库选图并明确「开始新修改」时，新建独立图片对话，保留原对话的各模式草稿；作为参考图添加时留在当前对话，不改变归属。
- [ ] C36 切换模型时保留兼容参数；对不兼容的画质、比例或分辨率明确提示重新选择，在全部设置有效前禁用发送，不静默降级或替换用户选择。
- [ ] C37 对话侧栏采用右侧占位布局，展开时收窄画布可视区域、不覆盖画布，保持当前缩放与位置；具体宽度通过三档桌面 UI 稿验证后确定。
- [ ] C38 未选中图片时仍允许打开「对话修改」；有当前项目的当前对话则恢复该对话，没有则展示选图入口，支持本地上传或素材库开始新修改，未选图前不能发送。
- [ ] C39 画布同时选中多张图片后点击「对话修改」，先让用户从中指定一张作为对话起点；不自动进入多图融合，不自动将其余选中图片加入输入。
- [ ] C40 多方案部分成功时，允许用户手动只重试明确失败项，保留成功结果、不重跑整轮，并在操作前提示可能产生额外费用；结果待确认项须先核对，不能直接重试。
- [ ] C41 第一版不提供删除历史轮次或清空整个对话的功能，保留追溯和继续修改能力；需要重新开始时使用「开始新修改」，不删除原对话。

## 四、验收矩阵 A01–A65

全部来自 PRD；每项完成后补证据路径，以下不是本轮测试结果。

- [ ] A01（P5）点击属性下方的对话修改：右侧展开；两个面板互斥，画布不重建。
- [ ] A02（P5）收起、重开或切换属性：当前对话、草稿与任务保留，焦点和键盘操作正常。
- [ ] A03（P6）单图 + 有效指令发送：请求只有指定一张图片；历史记录输入快照与结果。
- [ ] A04（P1）单图缺失或文字仅空白：不提交任务；显示可操作的缺失提示。
- [ ] A05（P6）多图调序后发送：用户看到的图号、历史快照、服务端和上游顺序一致。
- [ ] A06（P1）多图不足两张或超上限：前后端均拒绝，无额外生成调用。
- [ ] A07（P6）局部重绘已保存有效蒙版：mask 对应第一张底图；尺寸和格式符合约束。
- [ ] A08（P6）绘制蒙版后替换底图：原蒙版失效；未经重新绘制不可发送。
- [ ] A09（P7）第 5 轮后选第 2 轮继续修改：新轮以第 2 轮具体结果为输入，旧历史完整保留。
- [ ] A10（P5）模式来回切换：草稿按约定保留，其他模式的隐藏图片和蒙版不进入请求。
- [ ] A11（P4）双击发送、提交响应丢失：只有一个任务，恢复使用原请求号，不重复计费。
- [ ] A12（P7）生成中刷新或收起面板：原任务持续运行；重新打开正确恢复，事件重放不产生重复轮次。
- [ ] A13（P7）A 项目发起后切换到 B：上传、蒙版和结果只归属 A，不能写入 B；迟到回调遵守文档 epoch。
- [ ] A14（P4）成功、失败与未知结果：三者清楚区分，成功结果可查看/对比/下载/继续修改，未知结果不自动重发。
- [ ] A15（P4）当前对话历史与全局结果：同一任务和同一输出引用，不重复记账；既有结果恢复与操作保持有效。
- [ ] A16（P2）另一账号访问项目或结果：请求被既有授权规则拒绝，不能通过猜测标识读取图片或对话。
- [ ] A17（P5）1024/1280/1440 三档窗口：断言实际几何和操作：按钮不重叠、文字可读、输入可见、历史可滚动、无页面横向溢出。
- [ ] A18（P5）中文输入法与键盘发送：组合输入不误发，换行/发送快捷键与焦点恢复符合约定。
- [ ] A19（P8）已有节点工作流运行：AI 改款、局部重绘、属性编辑和全局结果功能无回归。
- [ ] A20（P7）同一画布分别修改图片 A 与 B：各自历史独立；返回 A 时可查看 A 的原有修改轮次，B 的轮次不混入 A。
- [ ] A21（P7）A 的对话加入 B 融合为 C，再继续修改 C：融合及后续轮次均追加在 A 的对话；B 的历史不变，C 不因生成而自动建立独立对话。
- [ ] A22（P5）在 A 中输入未发送要求，点击或拖动 B，再明确打开 B 的对话：普通选择不打断 A；点击「对话修改」后才切换为 B，重新打开 A 时原有草稿保留。
- [ ] A23（P7）连续生成多个版本后将其中一张加入画布：生成本身不新增节点；点击后仅加入所选结果，原图节点不被覆盖，其他版本仍可在历史中查看和继续修改。
- [ ] A24（P7）选中已加入画布的结果 C，再点击对话修改：恢复生成 C 的原对话历史，以 C 作为本轮底图，不新增独立对话；已有未发送草稿不能被静默覆盖。
- [ ] A25（P2）已提交多轮修改后刷新，或关闭项目再打开：恢复各图片的已提交指令、输入快照、任务状态和结果及其来源关系；不重新生成，不要求恢复未发送草稿。
- [ ] A26（P6）分别从画布、本地、素材库和当前对话历史选图：四种来源均可用于对应模式的输入；选图不触发生成，多输出节点可明确选定具体图片，添加参考图不切换对话归属。
- [ ] A27（P6）局部重绘分别不带参考图、带选填参考图运行：两者在底图、蒙版和指令有效时均可提交；底图始终为图 1，蒙版不作用于附加参考图，前后端检查用户图片总数并保留内部引导图名额。
- [ ] A28（P6）默认进入及手动切换模型后发送：默认 Sunburst；列表仅含当前模式可用模型，实际请求采用所选模型，历史记录实际模型；不支持的图片数量或参数在调用前被拦截。
- [ ] A29（P6）默认画质及手动调整画质后发送：初始为 medium，界面只提供所选模型支持的档位；请求和历史参数与用户选择一致，不自动升级到更高档位。
- [ ] A30（P6）输入明确的多方案要求，如「生成 3 张，分别为正面、侧面、微侧面」：无数量控件；形成对应的三个结果意图，共用本轮输入并按各自要求生成；同一轮分别展示标签与状态，任意成功图片均可继续修改或加入画布。
- [ ] A31（P4）多张生成部分成功、部分失败或未知：成功图片保留且可使用，历史准确显示各项状态和实际数量；刷新不重复创建结果或重跑整轮。
- [ ] A32（P3）输入「生成 5 张，分别为正面、侧面、微侧面」：先询问另外两张如何安排，不启动该轮图片生成；补充明确后只提交一次，保留原指令和补充回答，不擅自丢弃效果或改数量。
- [ ] A33（P3）用户明确授权「另外两张角度你来安排」：在授权范围内补足五张方案，保留已指定的三类效果，最终意图清单可追溯；明确请求无需再次逐轮确认。
- [ ] A34（P1）输入「分别生成正面、侧面、微侧面」，未写张数：自动形成三个结果意图，每种 1 张，共 3 张；不再询问数量，仍校验输出上限。
- [ ] A35（P1）单图指令包含改色、缩袖、保留背景等多项要求：视为同一张图片的组合要求，默认输出 1 张；不得机械地把每项修改拆成独立输出。
- [ ] A36（P1）明确指定 9 张，或未写张数但列出 9 种独立效果：提示单轮最多 8 张，请用户缩减或拆分；保留原指令，不启动图片生成、不截断效果、不自动分批。
- [ ] A37（P1）明确提出 5 张且各张意图完整，或授权补充到 5 张：保持请求数量为 5，不向 4 或 8 档取整，按五个意图执行并展示结果。
- [ ] A38（P6）单图/融合分别使用默认跟随及手动比例：默认按底图/图 1 显示方向计算比例，跟随状态下换图相应更新；手动选择合法比例后按该值提交，不因换图被静默覆盖。
- [ ] A39（P6）从自定义比例的融合模式切换到局部重绘：重绘使用底图比例，不提供改比例操作，不携带导致构图变化的旧比例；蒙版与实际提交底图尺寸一致。
- [ ] A40（P6）初始进入、切换 2K/4K 后发送：默认 2K；仅展示当前模型支持的分辨率档位，选择变化不自动修改画质；请求与历史保留实际分辨率和尺寸，局部重绘仍保持底图比例。
- [ ] A41（P4）同一对话生成时编辑下一轮草稿：草稿可编辑，发送受限；已提交轮次快照不被草稿修改，所有子任务终结后恢复发送能力，不自动发送草稿。
- [ ] A42（P4）多标签页同时向同一对话发送，或向不同图片对话发送：同一对话不能创建重叠活动轮次；不同对话按既有并发限制正常处理，服务端判断与 UI 状态一致。
- [ ] A43（P1）在上轮要求「保持背景不变」的结构图上输入「袖子再短一点」：使用选定结构图作为底图，本轮执行要求同时包含保留背景与缩短袖子；无需用户重写保持要求，不重复执行已完成的历史相对修改。
- [ ] A44（P1）此前要求保持背景，本轮明确要求「背景换成街景」：本轮以街景要求替换冲突的背景保持要求，其他有效要求不变；不额外要求解除旧约束或重复确认。
- [ ] A45（P1）本轮指令含糊，无法判断是否要替换既有要求：先提出针对性的澄清问题，明确前不擅自覆盖旧要求或提交图片生成。
- [ ] A46（P1）已生成到第 5 轮，重新选择第 2 轮结果继续修改：以第 2 轮结果为底图，仅继承该结果及其来源链上的有效要求，不带入第 3–5 轮要求；新结果留在同一图片对话，全部旧结果仍可查看和继续修改。
- [ ] A47（P5）在同一图片对话的三种模式中分别编辑文字、选图及局部重绘蒙版，再来回切换：每种模式恢复各自未发送草稿，内容互不覆盖；蒙版与原底图对应，隐藏模式输入不进入当前请求，切换和恢复均不自动发送；不要求刷新后恢复。
- [ ] A48（P5）当前模式已有草稿，选择另一张历史结果作底图后取消或关闭确认框：原底图、文字、选图和蒙版完整保留，不发送生成请求。
- [ ] A49（P5）确认「更换底图并清空当前模式草稿」：仅清空当前模式的文字、旧选图和蒙版并设置新底图；其他模式草稿、已有历史和运行中任务不变，不自动发送；局部重绘必须重新绘制蒙版。
- [ ] A50（P2）在同一项目内复制图片节点，并分别从原图与副本打开对话修改：定位到同一来源对话并展示同一历史，不创建重复会话；同一对话的活动轮次限制仍然生效。
- [ ] A51（P2）将副本重新上传替换为另一张图片后打开对话修改：新图片使用独立对话，不混入原历史或有效要求；原节点仍关联原对话，原历史完整保留。
- [ ] A52（P2）将原图或生成结果从项目 A 复制到项目 B，再打开对话修改：B 中建立独立对话，只使用复制图片，不载入 A 的历史或有效要求；两边后续轮次分别归属各自项目，A 的历史与任务不受影响。
- [ ] A53（P2）原对话已有草稿，从本地上传或素材库选图并选择「开始新修改」：新建独立图片对话，不继承旧历史或有效要求；原对话各模式草稿仍在本页保留，返回时可恢复，不自动生成。
- [ ] A54（P6）从本地上传或素材库将图片作为参考图添加：保持当前对话归属，按当前模式限制加入有序输入，不创建独立对话，也不自动发送。
- [ ] A55（P1）已选画质、比例或分辨率不被切换后的模型支持：兼容项保留，不兼容项明确提示重新选择；发送禁用，不静默降级，草稿内容保留。全部参数重新有效后，且其他发送条件满足时才恢复发送能力，不自动发送。
- [ ] A56（P1）绕过界面提交模型不支持的参数：服务端拒绝不兼容请求，不静默替换参数或调用真实生成；返回可理解的参数错误。
- [ ] A57（P5）在 1024、1280、1440 CSS 像素窗口下展开、收起对话侧栏并切换属性面板：实测侧栏与画布区域不重叠，画布可视宽度随占位变化；画布缩放与平移值保持不变，不触发 fitView、不重建画布、不清空节点选择，输入和历史操作可用。
- [ ] A58（P5）清除画布图片选择后打开对话修改，当前项目已有当前对话：恢复原当前对话及本页保留的草稿，不因没有画布选择而清空输入或阻止打开；发送仍按对话实际输入与任务状态校验。
- [ ] A59（P5）未选中图片且没有当前对话时打开对话修改：展示本地上传、素材库选图入口，选图前不能发送；选图并开始新修改后建立独立对话，不自动生成或载入其他项目的历史。
- [ ] A60（P5）画布同时选中多张图片后点击对话修改：先要求指定一张具体图片，再按来源定位或建立对话；不自动进入融合，不添加其他选中图片，不自动发送；涉及底图草稿冲突时仍需确认。
- [ ] A61（P5）在多选图片的对话起点选择中取消：当前对话、底图及各模式草稿均不变，不自动选用第一张图片或创建生成任务。
- [ ] A62（P4）请求 5 张、成功 3 张、明确失败 2 张，用户手动重试失败项：操作前展示范围与可能新增费用；只执行 2 个失败意图，沿用原提交输入及参数，保留 3 张成功图片和原失败记录，重试结果关联正确，不重跑整轮。
- [ ] A63（P4）部分输出处于结果待确认状态：不提供直接重试这些项的路径；先核对结果，不能因刷新、补齐或手动重试其他失败项而重新提交未知项。
- [ ] A64（P4）同一对话有活动轮次，或用户重复点击失败项重试：活动轮次结束前不能启动重叠重试；同一次重试防重复提交，当前草稿与成功结果不被修改。
- [ ] A65（P2）查看历史或使用「开始新修改」重新开始：第一版不提供历史轮次删除或整段对话清空操作；开始新修改后原对话历史仍保留，不影响查看和继续修改。未发送草稿清空与现有项目删除仍遵守各自规则。

## 五、最终交付关卡

- [ ] 全部 A01–A65 有可重现证据；失败/阻塞单独列出，不删验收项。
- [x] `pnpm run lint` / `pnpm run test` / `pnpm run check` / `pnpm run build` 通过并记录在验收报告。
- [x] 三档桌面 Dock 与结果操作 E2E 通过；IME、减少动画尚未逐条形成独立证据。
- [x] 现有工作流、姿势 API、局部重绘、全局结果、文档边界和权限回归通过。
- [x] CodeGraph sync/affected（包含新增源码）和完整 diff 检查完成。
- [x] `git diff --check` 及新增源码尾随空白检查通过。
- [ ] 本地评审记录精确 base/head；所有可操作 P0–P3 问题处理完毕。
- [x] 明确记载 gate:codex 因 GitNexus 依赖未运行；不报告完整 gate 通过。
- [ ] 数据库迁移、回退和在途任务兼容性说明完成；未对生产执行。
- [ ] 用户手册/文档状态更新，只声明实际完成的范围。
- [ ] 如需要，单独取得真实 AI 验收授权并记录效果与费用；未授权则明确未验证。
- [ ] 提交/推送、合并、发布、部署分别按授权执行；未授权保持未执行。

## 六、执行记录

| 日期 | 工作包/验收项 | 实际工作量 | 证据 | 状态/阻塞 |
| --- | --- | --- | --- | --- |
| 2026-09-21 | P2 PostgreSQL 对话存储、身份与授权 | 已完成 | `pnpm run test`：隔离 PostgreSQL 全量通过；最终 CodeGraph affected 显式覆盖 61 个源码/测试文件并遍历 299 个依赖节点；`pnpm run build`、`pnpm run lint`、`git diff --check` 通过 | 未运行 gate:codex（规则禁止 GitNexus 门禁） |
| 2026-09-21 | P3 结构化规划、澄清与意图快照 | 已完成 | `pnpm run test`：planner 通过、API 5 项通过；ready 只创建意图快照，不创建 generation run；最终 CodeGraph `status/sync/affected` 已完成 | 未验证真实文本模型能力与成本 |
| 2026-09-21 | P5 前端状态、右侧 Dock 与三档桌面布局 | 已完成 | `pnpm exec tsx` 图片对话 UI/输入/恢复契约通过；`pnpm run build` 通过；对话 Dock 与结果操作 E2E 1024/1280/1440 通过，覆盖换底图确认取消/确认 | IME、减少动画未逐条实测 |
| 2026-09-21 | P6 输入来源、素材库、上传与蒙版 | 已完成 | 输入/蒙版专项及全量中对话测试通过；上传、MaskEditor、素材引用和 DocumentTarget 门禁已接通 | 未做真实 Provider 效果和生产文件存储验收 |
| 2026-09-21 | P7 历史恢复、结果操作与澄清联调 | 已完成 | `image-conversation-recovery/queue/storage/api` 通过；build、持久化历史与结果操作三档 E2E、CodeGraph `affected` 通过；结果操作 E2E 验证继续修改只更新草稿、加入画布保留原图并写入稳定来源元数据 | 真实模型、真实费用、A01–A65 逐条矩阵未完成 |
| 2026-09-21 | P1/P5 参数能力与多选对话起点 | 本地实现与回归完成 | 共享参数校验在规划前和入队前拦截；按模式生成模型列表；多选起点 E2E setup + 1024/1280/1440 共 4/4，覆盖草稿底图冲突取消、保留草稿、无对话请求和双选保留；全量 `test/check/build` 通过 | 真实 Provider 参数能力和 A01–A65 逐条证据未完成 |
| 2026-09-21 | P2/P5/P7 历史快照与来源预览恢复 | 本地实现与回归完成 | `ConversationHistory` 显示有序输入/蒙版快照、来源轮次与折叠参数；服务端按 owner/project 解析 `asset/<id>` 预览；`/resolve` 返回完整历史；标准隔离 PostgreSQL runner、持久化历史与结果操作三档 E2E（各 4/4）和相关对话专项通过；结果操作覆盖继续修改、显式加入画布、原图保留和稳定来源元数据 | 真实 Provider 效果、截图/trace 和 A01–A65 逐条证据未完成 |
| 2026-09-21 | P8 交付审查 | 本地技术门禁完成 | `pnpm run lint/test/check/build`、对话 Dock/多选起点/持久化历史/结果操作三档 E2E（13/13）、`git diff --check`、CodeGraph `status/sync/affected` 均通过；验收报告已更新 | A01–A65 逐条矩阵、真实 Provider 效果/费用、gate:codex、生产迁移与发布未执行 |
| 2026-09-21 | P2/P5 来源隔离与结果操作窄屏收口 | 本地实现与回归完成 | 新增同/跨项目剪贴板来源 E2E；结果操作 E2E 新增底图冲突取消/确认路径；1024px 下结果操作栏改为三列网格，三档组合回归 setup + 6 个场景共 7/7 通过，未触发对话或 AI 请求 | A50–A52 完整历史/后续轮次矩阵、真实 Provider 效果/费用、A01–A65 逐条证据未完成 |
| 2026-09-21 | P5 模式草稿与异步会话建立收口 | 本地实现与回归完成 | `imageConversationStore` 保留未绑定会话在异步解析窗口内的脏草稿；UI 状态契约新增覆盖；模式草稿和“开始新修改”三档 E2E setup + 两个场景共 7/7 通过 | IME、减少动画、真实 Provider 效果/费用和 A01–A65 逐条证据未完成 |

本记录不代表真实模型效果、正式部署或提交/推送/合并完成；A01–A65 逐条证据和真实 Provider 验收仍需单独授权与执行。
