# 色彩工具：第 1 张「内嵌轻星」验收（2026-09-11）

- 范围：所有色系、我的收藏、自定义颜色、最近使用的收藏星形；保留现有面板布局与业务状态。
- source visual truth: `/Users/neihe/.codex/generated_images/01a0899e-475a-7822-8a65-fc80c52635e1/exec-7bdd8e56-0449-40a4-adaa-f8d9f94cfe76.png`（1254×1254 设计板）。
- implementation screenshot: `/tmp/neihe-star-option1-preview.png`（1280×900 桌面视口）；局部：`/tmp/neihe-star-option1-detail.png`。
- 源图为放大展示，不将标题、巨型色块或展示板背景移植到产品。以色块宽度归一比较右上角星形位置和遮挡比例；实际色块高 40 CSS px，星形 10×10 CSS px，距色面上/右边缘 4px；保留 24px 透明点击区域。
- 全图检查面板与色块布局；源图和实现局部截图在同一视觉输入中对照，未发现需要修复的 P0/P1/P2。局部截图由浏览器裁图接口输出，其缩放不同于全图，精确尺寸以三种桌面宽度的 DOM 几何断言为准。
- 状态：未收藏空心星、已收藏项目金色填充；深色面用浅色轮廓、浅色面用深色轮廓。无星形底板，悬停不恢复底板。
- 字体/文案：保留现有色彩工具的字体、标签与中文说明，设计板标题不属于产品内容。
- 间距/布局：保持八列色块、原面板尺寸；星形在色块内部右上角，不占单独一列。
- 色彩/资产：沿用 `--gc-accent` 填充收藏态；源图的浅/深轮廓映射到实色色面。复用项目 Lucide StarIcon，无新增栅格资产或手绘 SVG。
- 验证：1024/1280/1440 桌面几何、全部分类、收藏与选色隔离、键盘焦点、收藏同步和取色由聚焦 E2E 覆盖。初次运行在切换分类期间采到已卸载元素的零尺寸；补充等待分类选中并重试完整几何断言，未放宽尺寸要求。
- 预览页面为无账户隔离展示，不代表真实收藏保存；持久化/收藏交互通过隔离 E2E 验证。页面控制台检查无 error。
- `npm run check`、`npm run build` 已通过；CodeGraph sync/affected 与 diff 检查完成。`gate:codex` 仍依赖 GitNexus，未运行，不能宣称完整门禁通过。
- 未提交、未推送、未部署；保留原有部署文件暂存修改。

final result: passed

---

# 历史：生成结果节点设计验收

## 范围与视觉依据

- 仅调整生成结果节点；其余节点、画布主题和业务模型不变。
- source visual truth: `/var/folders/7m/7st5h2m55yvg30rzwdtzt_700000gn/T/codex-clipboard-81a89ea7-9206-4555-a990-a10b19cf29a1.png`
- 用户明确要求底部“继续处理”替换为“备注”；点击弹出输入栏，确定后保存。
- 实现截图：`test-results/result-node.workbench-resu-6c53b-e-save-persist-after-reload-desktop-1280/result-node.png`
- 弹窗截图：`test-results/result-node.workbench-resu-6c53b-e-save-persist-after-reload-desktop-1280/result-note-dialog.png`
- 本地隔离预览：`http://127.0.0.1:5188/e2e/fixtures/result-node.html`。预览使用真实组件，但保存为演示，不写正式项目；实际 API 保存由隔离 PostgreSQL E2E 验证。

## 比较口径

- 源图为 236×382 像素，内部卡片约 220 像素宽；实现沿用项目 280 CSS px 节点宽度，截图约 280×264 像素，deviceScaleFactor=1、画布缩放 100%。以卡片宽度约 280/220=1.273 的比例比较标题与按钮，不把图片长宽比不同误判为布局漂移。
- 桌面验收视口：1024×768、1280×720、1440×900。无移动端布局改动。
- 状态：单图、未选中节点；另验收选中工具栏、多图选择、备注编辑/取消/确定保存/失败重试/只读。
- 全图比较：将用户参考图、浏览器实际节点截图及弹窗截图放入同一视觉检查输入进行对照。节点裁图已经是聚焦区域，标题和全部按钮可读，无须再次裁切。
- 图片是业务生成内容，不是要替换的设计素材。测试使用项目现有场景图片，故与参考人像的内容、比例不同；保持原图完整、不拉伸、不裁剪。没有修改用户的生成图片。
- 源图未定义备注弹窗外观；使用项目已有 shadcn Dialog / Textarea / Button 及现有深色面板主题，不引入另一套主题。

## 五项视觉检查

1. 字体：沿用中文系统字体；标题 12px、中等字重，底部文字 11px、常规字重。标题左对齐且在卡内，保留双击改名。
2. 布局：紧凑标题栏、整图、四列等宽操作；卡片圆角 5px，媒体/按钮圆角 4px。照片保持固有比例，高图不超过 440px；多图用附加缩略条，不丢失任何结果。
3. 色彩：白色节点、细中性边框、深灰文字，沿用 `--gc-node-*`。原有选中、成功、失败及未知结果状态仍由 NodeFrame 的状态边框/外环表达。
4. 图像与图标：真实媒体引用、`object-fit: contain`；使用现有线性图标库对应查看/对比/下载/备注/更多，不制作伪图像或替换原媒体。
5. 文案：底部严格为“查看 / 对比 / 下载 / 备注”。弹窗为“结果备注”，按钮为“取消 / 确定保存”；保存失败明确提示并允许重试。

## 比较迭代与发现

- [P2，已修正] 初次浏览器检查发现标题仍受原 NodeFrame 的 Tailwind 独立 `translate` 属性影响，落到卡片左侧。仅在结果节点样式中增加 `translate: none`，保留其他节点原布局；后续浏览器截图和标题边界断言通过。
- [证据采集，已复核] 第一组 E2E 弹窗截图捕获在打开动画中途，不能用来判断最终颜色/对比度。增加等待弹窗 opacity=1 的截图条件，重跑后将稳定截图与源图及节点裁图放入同一比较输入复核：弹窗背景不透明、文字清晰、焦点可见、操作区无重叠；无未解决 P0/P1/P2。
- 已确认的预期差异：节点沿用现有 280px 宽度；示例图片内容/比例不同；备注弹窗沿用项目深色面板。它们不是本次视觉缺陷。

## 交互与回归

- 已通过三种桌面宽度下 9 项浏览器用例及 1 项认证准备：取消不写入、显式保存并刷新回读、清空、Escape/焦点恢复、保存失败重试、只读保护、多图查看/下载/对比、选中第二张图的下游风格转绘连线仍为 `image:1`、同页签替换文档不串写。
- 保存绑定 `tabId + projectId + documentEpoch`；草稿与弹窗状态只存组件本地。
- E2E 页面异常收集为空；测试明确阻止真实 AI 生成请求。
- 文件夹保存/自动保存移至右上角；原有处理工具栏和结果连线保留。真实系统文件夹权限弹窗、真实视频解码未在本次新增浏览器用例中验证。
- CodeGraph affected 指向 `e2e/result-node.workbench.spec.ts` 和 `tests/flow-history.test.ts`；另补跑文档快照、文本事务、项目页签、结果导出及全量本地检查。图覆盖不视为穷尽。
- `gate:codex` 仍依赖 GitNexus，按项目规则未运行，不能宣称完整门禁通过。
- 最终 `npm run check`、`npm run build`、`npm run lint`、`git diff --check` 均通过；执行环境为 Node.js v24.20.0，未冒充最低版本门禁结果。完整日志位于 `/tmp/neihe-result-node-check.log`、`/tmp/neihe-result-node-build.log`、`/tmp/neihe-result-node-e2e.log`。

## Implementation Checklist

- [x] 结果节点视觉与备注功能实现。
- [x] 桌面布局与实际项目保存测试。
- [x] 原有媒体操作回归。
- [x] 稳定弹窗截图复核。

## Follow-up Polish

无额外建议纳入本次范围。

final result: passed
